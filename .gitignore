# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI
# Agent skills live in .agents/ and are symlinked into .claude/ — both are
# tracked so a fresh clone has them without re-running the skills CLI.
# Only machine-local agent settings are ignored.
.claude/settings.local.json

# Local git worktrees created by Claude Code
.claude/worktrees/
